{
	"name": "@dump-txt/rig",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"license": "MIT",
	"author": "Matt Cavender",
	"exports": {
		".": "./src/index.ts",
		"./styles.css": "./src/styles.css"
	},
	"scripts": {
		"check": "concurrently --group \"eslint . --no-cache -f unix\" \"tsc --noEmit --pretty false\"",
		"fix": "eslint . --fix --cache",
		"unit": "vitest run unit",
		"integration": "vitest run --environment jsdom integration"
	},
	"dependencies": {
		"@dump-txt/ui": "*"
	},
	"devDependencies": {
		"jsdom": "30.0.1",
		"vitest": "5.0.0"
	}
}
